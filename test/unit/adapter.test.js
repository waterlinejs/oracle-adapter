/*
import assert from 'assert'
import adapter from '../../dist/adapter'

describe('adapter tests', function() {
  this.timeout(60000)
  it('should work', (done) => {
    // register the connection
    console.log('registering the connection')
    adapter.registerConnection({
      url: '192.168.0.8',
      user: 'oracleuser',
      password: 'l8ter'
    }, {}, (err, conn) => {
      conn.execute('select * from student', [], function(err, res) {
        assert(false)
        done()
      })
    })
  })
})
*/
